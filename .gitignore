# OS noise
Thumbs.db
desktop.ini
.DS_Store

# Editor folders
.vscode/
.idea/
*.swp
*~

# Secrets (none used currently, but never commit them)
.env
.env.local
.env.*.local

# Local-only notes and tooling (never deploy publicly)
dispute-log.txt
setup-form.txt
setup-notifications.txt
icon-source.html
review/
